--- linux-2.6.12.3/drivers/i2c/chips/Kconfig	2005-07-15 17:18:57.000000000 -0400
+++ linux-2.6.12.3-qnap/drivers/i2c/chips/Kconfig	2007-02-17 14:10:17.000000000 -0500
@@ -440,4 +440,21 @@
 	  This driver can also be built as a module.  If so, the module
 	  will be called m41t00.
 
+config SENSORS_RS5C372A
+	tristate "RICOH RS5C372A RTC chip"
+	depends on I2C && PPC32 && SANDPOINT
+	help
+	  If you say yes here you get support for the RICOH RS5C372A RTC chip.
+
+	  This driver can also be built as a module.  If so, the module
+	  will be called rs5c372a.
+
+config SENSORS_S35390A
+	tristate "S-35390A RTC chip"
+	depends on I2C && PPC32 && SANDPOINT
+	help
+	  If you say yes here you get support for the S-35390A RTC chip/
+	  
+	  This driver can also be built as a module.  If so, the module
+	  will be called i2c-s3531a
 endmenu
